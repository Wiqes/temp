import { useStyles } from '@components/Plan/style';

export const Plan = () => {
  const classes = useStyles();

  return (
    <div className={classes.root}>
      <div>Plan</div>
    </div>
  );
};
