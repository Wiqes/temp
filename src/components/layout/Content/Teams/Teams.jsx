import React from "react";

import { useStyles } from "@content/Teams/style";

const Teams = () => {
  const classes = useStyles();

  return (
    <div className={classes.root}>
      <div>Teams</div>
    </div>
  );
};

export { Teams };
